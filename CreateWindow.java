import javax.swing.*;

public class CreateWindow {
    JFrame newWindow(JFrame window) {
        window.setSize(800, 600);
        window.setLayout(null);
        window.setVisible(true);
        return window;
    }
}
